# Frontend env — copy the matching environment file. Never commit real values.
# Production builds MUST set an HTTPS origin. Localhost is not a production fallback.

# Development (explicit):
# EXPO_PUBLIC_API_URL=http://127.0.0.1:4000

# Production (required):
EXPO_PUBLIC_API_URL=https://YOUR_PRODUCTION_BACKEND_DOMAIN
