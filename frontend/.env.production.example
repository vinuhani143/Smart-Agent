# Production — HTTPS origin of the MusicMix API. Do not use localhost.
# Replace the placeholder before a release build. Never commit a real domain secret.
EXPO_PUBLIC_API_URL=https://YOUR_PRODUCTION_BACKEND_DOMAIN
